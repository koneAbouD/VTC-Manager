spring:
  flyway:
    enabled: true
    baseline-on-migrate: true
    out-of-order: true
    ignore-migration-patterns: "*:missing"
    locations: classpath:db/migration/flyway/schema,classpath:db/migration/flyway/jdd/test

logging:
  level:
    org.flywaydb: DEBUG
