<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Theme applied to the Android Window while the process is starting when the OS's Dark Mode setting is off -->
    <style name="LaunchTheme" parent="@android:style/Theme.Light.NoTitleBar">
        <!-- Show a splash screen on the activity. Automatically removed when
             the Flutter engine draws its first frame -->
        <item name="android:windowBackground">@drawable/launch_background</item>
    </style>
    <!-- Theme applied to the Android Window as soon as the process has started.
         This theme determines the color of the Android Window while your
         Flutter UI initializes, as well as behind your Flutter UI while its
         running.

         This Theme is only used starting with V2 of Flutter's Android embedding. -->
    <style name="NormalTheme" parent="@android:style/Theme.Light.NoTitleBar">
        <item name="android:windowBackground">@color/splash_background</item>
    </style>

    <!-- Thème de l'écran de rognage uCrop (image_cropper).
         - Barres système opaques et distinctes (l'app cible SDK 36 → l'edge-to-edge
           est forcé ; on l'annule pour cette activité afin que l'écran ne se
           confonde plus avec la barre de statut et la barre de navigation).
         - Animation d'entrée/sortie standard, plus fluide que le thème par défaut. -->
    <style name="Theme.VtcUcrop" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorPrimary">@color/ucrop_toolbar</item>
        <item name="colorPrimaryDark">@color/ucrop_status_bar</item>
        <item name="colorAccent">@color/ucrop_accent</item>
        <item name="android:statusBarColor">@color/ucrop_status_bar</item>
        <item name="android:navigationBarColor">@color/ucrop_nav_bar</item>
        <!-- Chrome sombre (#20242F) en haut comme en bas → icônes système claires. -->
        <item name="android:windowLightStatusBar">false</item>
        <item name="android:windowLightNavigationBar">false</item>
        <item name="android:windowAnimationStyle">@android:style/Animation.Activity</item>
        <!-- Android 15+ (API 35) : rétablit les barres système opaques pour cette
             activité au lieu de l'edge-to-edge imposé par la plateforme. -->
        <item name="android:windowOptOutEdgeToEdgeEnforcement">true</item>
    </style>
</resources>
